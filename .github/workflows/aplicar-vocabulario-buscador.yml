name: Aplicar Vocabulario con buscador protagonista

on:
  push:
    branches:
      - develop
    paths:
      - .github/workflows/aplicar-vocabulario-buscador.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  aplicar-vocabulario:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar develop
        uses: actions/checkout@v4
        with:
          ref: develop
          fetch-depth: 0

      - name: Preparar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Aplicar diseño aprobado
        run: python scripts/aplicar_vocabulario_buscador_protagonista.py

      - name: Validar cambios
        shell: bash
        run: |
          set -euo pipefail
          grep -q 'id="vocabulario-buscador-protagonista"' index.html
          grep -q 'vocabBuscadorEntrada' index.html
          grep -q 'id="bloqueBuscadorCategorias"' index.html
          grep -q 'video/lspedia_transparente.webm' index.html
          grep -q 'img/avatar_lupa.webm' index.html
          grep -q 'const VERSION_APP = "v32";' sw.js
          git diff --check

      - name: Guardar en develop
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html sw.js
          git commit -m "Compactar Vocabulario y destacar buscador"
          git push origin HEAD:develop
